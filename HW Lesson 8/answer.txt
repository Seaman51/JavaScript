Не выполняя код, ответить, что выведет браузер и почему:

Задание: 
if (!("a" in window)) {
    var a = 1;
}
alert(a);
Ответ:
undefined
так как интерпретатор идет следующим образом:
var a
"a" in window - true
!True - false
if false - условие лож
вывести a - переменная a обьявлена, но не определена

Задание:
var b = function a(x) {
    x && a(--x);
};
alert(a);
Ответ:
Выдаст тело функции, сама функция без смысла

Задание:
function a(x) {
    return x * 2;
}
var a;
alert(a);
Ответ:
Выдаст тело функции, никакой аргумент в функцию не был передан,
если запросить alert(a(4)); - выдаст 8
Объявление функции переопределяет объявление переменной при подъеме

Задание:
function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);
Ответ:
10
не смотря на то, что при вызове функции b() в качестве аргумента (а) задано значение 3, 
внутри функции есть присваивание а = 10, 


Задание:
function a() {
    alert(this);
}
a.call(null);
Ответ:
строковый эквивалент обьекта null
В this находится ссылка на a